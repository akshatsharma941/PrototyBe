{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "lint:content": "node scripts/lint-content.js",
    "lint:content:json": "node scripts/lint-content.js --json",
    "verify:controller-acceptance": "node scripts/verify-controller-acceptance.js",
    "verify:pick-next-phase": "node scripts/verify-pick-next-phase.js",
    "verify:controller-override": "node scripts/verify-controller-override.js",
    "pretest": "npm run lint:content && npm run verify:controller-acceptance && npm run verify:pick-next-phase && npm run verify:controller-override",
    "test": "echo \"Tests run as pretest (lint:content + 3 verifiers)\" && exit 0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "axios": "^1.18.1",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mongodb-memory-server": "^11.2.0",
    "mongoose": "^9.7.4"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
